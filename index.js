

// Event listeners for navigation

var Register= document. getElementById('Register-button').addEventListener('mouseover', (e)=> {});
    // Show login form initially
    Register=(e)=>{

    };

    var login=document.getElementById('Login-button').addEventListener('mouseover',  (e)=> {
        
    });
    login=(e)=>{e.add(login-form)};

    

//
document.getElementById('Register-button').addEventListener('click', function () {
    showRegistrationForm();
});
